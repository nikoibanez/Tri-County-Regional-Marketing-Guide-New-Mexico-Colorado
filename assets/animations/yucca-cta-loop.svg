<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 120 120" role="img" aria-labelledby="ctaLoopTitle" data-brand-palette="raton-mainstreet">
  <title id="ctaLoopTitle">Forward loop marker</title>
  <defs>
    <style>
      .cta-pulse {
        animation: cta-pulse 4.2s ease-in-out infinite;
        transform-origin: 60px 60px;
      }
      .orbit {
        fill: none;
        stroke: rgba(34,73,90,0.24);
        stroke-width: 4;
      }
      .arrow {
        fill: none;
        stroke: #772816;
        stroke-width: 7;
        stroke-linecap: round;
        stroke-linejoin: round;
      }
      .node {
        fill: #7ed4d8;
        stroke: #d97345;
        stroke-width: 3;
      }
      @keyframes cta-pulse {
        0%, 100% { transform: translateX(0) scale(1); opacity: 0.84; }
        50% { transform: translateX(4px) scale(1.04); opacity: 1; }
      }
      @media (prefers-reduced-motion: reduce) {
        .cta-pulse {
          animation: none !important;
        }
      }
    </style>
  </defs>
  <circle class="orbit" cx="60" cy="60" r="42"/>
  <g class="cta-pulse">
    <path class="arrow" d="M42 60h34"/>
    <path class="arrow" d="M66 43l18 17-18 17"/>
    <circle class="node" cx="36" cy="60" r="8"/>
  </g>
</svg>
